{
	"label": "Better Font Rendering",
	"description": "Improve the font rendering for better readability",
	"category": "Tweaks",
	"scripts": {
		"exec": {
			"label": "Set up",
			"script": "run-as-root dnf -y install ozon-font-config"
		},
		"undo": {
			"label": "Revert",
			"script": "run-as-root dnf -y --setopt clean_requirements_on_remove=1 erase ozon-font-config"
		},
		"status": { "script": "rpm --quiet --query ozon-font-config" }
	}
}
