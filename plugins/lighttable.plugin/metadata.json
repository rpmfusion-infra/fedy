{
	"icon": "lticon",
	"label": "LightTable",
	"description": "The next generation code editor.",
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "test -f /opt/LightTable/LightTable" }
	}
}
