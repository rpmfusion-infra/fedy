{
	"icon": "wxHexEditor",
	"label": "wxHexEditor",
	"description": "Hex editor with support for large file and low level data",
	"license": "GPLv2",
	"category": "Development Tools",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query wxHexEditor" }
	}
}
