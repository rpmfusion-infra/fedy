{
	"icon": "steam",
	"label": "Steam",
	"description": "The ultimate online game platform.",
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root dnf -y install steam"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root dnf -y --setopt clean_requirements_on_remove=1 erase steam"
		},
		"status": { "command": "rpm --quiet --query steam" }
	}
}
