{
	"icon": "tdiff",
	"label": "Tdiff",
	"description": "Tree diff - diff, with mode, owner, permissions and other inode attributes ",
	"license": "GPLv3",
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query tdiff" }
	}
}
