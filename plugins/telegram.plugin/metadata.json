{
	"icon": "telegram-desktop",
	"label": "Telegram",
	"description": "A messaging app with a focus on speed and security.",
	"license": "GPLv2",
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root dnf -y install telegram-desktop"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root dnf -y remove telegram-desktop"
		},
		"status": { "command": "rpm --quiet --query telegram-desktop" }
	}
}
