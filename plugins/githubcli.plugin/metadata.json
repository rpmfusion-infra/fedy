{
	"icon": "githubcli",
	"label": "GitHub Cli",
	"description": "GitHub on the command line",
	"license": "MIT License",
	"category": "Development Tools",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query gh" }
	}
}
