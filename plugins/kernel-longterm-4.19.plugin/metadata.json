{
	"icon": "",
	"label": "kernel-longterm 4.19",
	"description": "kernel longterm support 4.19",
	"license": "GPLv2",
	"category": "Drivers",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s remove.sh "
		},
		"status": { "command": "rpm --quiet --query kernel-longterm" }
	}
}
