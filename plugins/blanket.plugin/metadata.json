{
	"icon": "blanket",
	"label": "Blanket",
	"description": "Listen to different sounds",
	"license": "Multi",
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query blanket" }
	}
}
