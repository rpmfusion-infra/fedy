{
	"icon": "",
	"label": "Fuse exFAT driver",
	"description": "Allow to mount exFAT filesystems",
	"license": "GPLv2+",
	"category": "Drivers",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s remove.sh "
		},
		"status": { "command": "rpm --quiet --query fuse-exfat" }
	}
}
