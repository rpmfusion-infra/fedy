{
	"icon": "evopop",
	"label": "EvoPop themes",
	"description": "GTK and icon themes from the Solus Project.",
	"license": "GPLv3",
	"category": "Themes",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query EvoPop-theme EvoPop-Azure-theme" }
	}
}
